import { formatearPrecio } from "../../utils/formatearPrecio.js";
const txt = v => v?.replaceAll("_", " ") || "—";
export default function TablaPagos({ pagos, onEditar }) { if (!pagos.length) return <div className="gestion-vacio">No hay pagos registrados.</div>; return <div className="gestion-tabla-wrap"><table className="gestion-tabla"><thead><tr><th>Pago</th><th>Pedido</th><th>Método</th><th>Monto</th><th>Estado</th><th>Referencia</th><th></th></tr></thead><tbody>{pagos.map(p => <tr key={p._id}><td className="gestion-id">#{p._id.slice(-6).toUpperCase()}</td><td>{p.pedidoId?._id ? `#${p.pedidoId._id.slice(-6).toUpperCase()}` : "—"}</td><td>{txt(p.metodo)}</td><td>{formatearPrecio(p.monto)}</td><td><span className={`gestion-badge gestion-badge--${p.estado}`}>{txt(p.estado)}</span></td><td>{p.referenciaTransaccion || "—"}</td><td><button className="gestion-link" onClick={() => onEditar(p)}>Estado</button></td></tr>)}</tbody></table></div> }
